image: node:latest

stages:
  - build
  - test

cache:
  paths:
    - ventoli-model/node_modules/
    - ventoli-front/node_modules/
    - ventoli-api/node_modules/

install_dependencies:
  stage: build
  script:
    - cd ventoli-model
    - npm install
    - cd ..
    - cd ventoli-front
    - npm install
    - cd ..
    - cd ventoli-api
    - npm install
  artifacts:
    paths:
      - ventoli-model/node_modules/
      - ventoli-front/node_modules/
      - ventoli-api/node_modules/

run_tests:
  stage: test
  script:
    - cd ventoli-model
    - npm run test
    - cd ..
    - cd ventoli-front
    - npm run test
    - cd ..
    - cd ventoli-api
    - npm run test
